name: Together alias real runtime proof

on:
  push:
    branches:
      - proof/together-alias-runtime-20260912

permissions:
  contents: read

concurrency:
  group: together-alias-runtime-proof
  cancel-in-progress: true

jobs:
  proof:
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    env:
      PR_SOURCE_HEAD: 1e684d83dce67807131b049529e919a773ab6dab
      RESULTS_DIR: artifacts/together-alias-runtime
    steps:
      - name: Checkout immutable proof branch
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Verify exact-head lineage and proof-only delta
        shell: bash
        run: |
          set -euo pipefail
          git merge-base --is-ancestor "$PR_SOURCE_HEAD" HEAD
          mapfile -t changed < <(git diff --name-only "$PR_SOURCE_HEAD"...HEAD)
          printf '%s\n' "${changed[@]}"
          test "${#changed[@]}" -eq 2
          test "${changed[0]}" = ".github/proof/together-alias-runtime.mjs"
          test "${changed[1]}" = ".github/workflows/proof-together-alias-runtime.yml"

      - name: Set up exact PR source dependencies
        uses: ./.github/actions/setup-node-env
        with:
          node-version: 24.x
          install-bun: "false"
          install-deps: "true"
          frozen-lockfile: "true"
          cache-mode: restore
          dependency-cache: "true"

      - name: Build exact PR source
        run: pnpm build

      - name: Exercise the saved alias through the real CLI and transport
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p "$RESULTS_DIR"
          PR_SOURCE_HEAD="$PR_SOURCE_HEAD" \
            PROOF_OUTPUT="$RESULTS_DIR/result.json" \
            node .github/proof/together-alias-runtime.mjs

      - name: Publish summary
        shell: bash
        run: |
          node --input-type=module <<'NODE'
          import fs from "node:fs";
          const result = JSON.parse(fs.readFileSync(`${process.env.RESULTS_DIR}/result.json`, "utf8"));
          const lines = [
            "## Together alias real-runtime proof",
            "",
            `- Exact PR head: \`${result.exactHead}\``,
            `- \`${result.provider}\`: models status/list accepted the alias and model \`${result.listedModel}\`.`,
            `- Local agent used the saved base URL and header, reached \`${result.resolvedTransport.path}\`, and returned \`${result.responseObserved}\`.`,
            "- Secretless: loopback provider only; no Together API call or repository secret.",
          ];
          fs.appendFileSync(process.env.GITHUB_STEP_SUMMARY, `${lines.join("\n")}\n`);
          console.log(lines.join("\n"));
          NODE

      - name: Upload immutable proof record
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: together-alias-runtime-proof
          path: ${{ env.RESULTS_DIR }}
          if-no-files-found: error
          retention-days: 30
